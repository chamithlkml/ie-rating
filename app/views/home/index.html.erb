<div class="container text-center">
  <div class="row">
    <div class="col">
      <% if user_signed_in? %>
        
      <% else %>
        <div class="btn-group" role="group">
          <a type="button" href="<%= new_user_registration_path %>" class="btn btn-outline-primary">Sign up</a>
          <a type="button" href="<%= new_user_session_path %>" class="btn btn-outline-primary">Sign in</a>
        </div>
      <% end %>
    </div>
  </div>
</div>